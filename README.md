# GDY Music Academy

Static website for **GDY Music Academy**.

## Run locally

Open `index.html` directly, or run a local server:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.

